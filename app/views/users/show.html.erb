<h3><%= current_user.name %>のマイページ</h3>

<h3>所属グループ：<%= link_to current_user.group.name, group_path(current_user.group_id) %></h3>
<%= link_to I18n.t('views.button.delete'), user_path(current_user.id), method: :delete, data: { confirm: '退会しますか？' }, class: 'btn btn-sm btn-danger' %></td>
<%= link_to '戻る', groups_path, class: 'btn btn-sm btn-primary' %>