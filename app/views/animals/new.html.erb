<p id="notice"><%= notice %></p>
  
<h2><%= '天使新規作成' %></h2>

<%= render 'form', animal: @animal %>
<div class="btm-buttons">
  <%= link_to '戻る', group_path(current_user.group_id), class: 'btn btn-light' %>
</div>