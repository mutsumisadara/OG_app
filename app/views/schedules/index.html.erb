<h1>スケジュール</h1>
<%= month_calendar schedules: @schedules do |date, schedules| %>
    <%= date.day %>
  
    <% schedules.each do |schedule| %>
      <div>
        <%= schedule.content %>
      </div>
    <% end %>
  <% end %>
  
  <%= link_to 'スケジュール追加', new_schedule_path %>